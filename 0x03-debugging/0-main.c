#include "main.h"
void positive_or_negative(int i);
/**
* main - examines function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
int i;

i = 2;
positive_or_negative(i);

return (0);
}
